{
  "name": "nameless-cemetery-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && npx wrangler pages deploy dist --project-name nameless-cemetery --branch main"
  },
  "dependencies": {
    "astro": "^5.3.0",
    "@astrojs/sitemap": "^3.2.0"
  }
}
